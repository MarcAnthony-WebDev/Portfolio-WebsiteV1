import { createUseStyles } from 'react-jss';

export const styles = createUseStyles({
  asteroid: {
    padding: '4rem',
    backgroundColor: 'royalblue',
    color: 'white',
    borderRadius: '50%',
    zIndex: '20',
    position: 'absolute',
  },
});
